CONNECT SYS/XXXX@SBDB2 AS SYSDBA;
SHUTDOWN IMMEDIATE;
STARTUP RESTRICT;
BEGIN
	FOR JOB IN (SELECT JOB FROM DBA_JOBS) LOOP
	 	SYS.dbms_ijob.BROKEN(JOB.JOB,TRUE);
	END LOOP;
END;
/
COMMIT;
SHUTDOWN IMMEDIATE;
STARTUP MOUNT;
ALTER DATABASE OPEN READ ONLY;
